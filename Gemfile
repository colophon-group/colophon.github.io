source "https://rubygems.org"

# Match GitHub Pages' environment
gem "github-pages", group: :jekyll_plugins

# Plugin (already in _config.yml)
gem "jekyll-include-cache"

# Needed on Ruby 3+
gem "webrick", "~> 1.8"
